{
  "name": "eqindex-api",
  "version": "0.1.0",
  "private": true,
  "description": "EQIndex public API (Express) — reads rankings/views from Postgres",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test smoke.test.mjs",
    "test:smoke": "node --test smoke.test.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
